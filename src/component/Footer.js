import React from 'react'; // 프로퍼티 타입을 지정해주기 위해 사용 한다.

function Footer(props) { 
    return (
        <div>
            <footer>
                <h1>푸터입니다.</h1>
            </footer>
        </div>
    );
}

export default Footer; //다른 JS파일에서 불러올 수 있도록 내보내주기